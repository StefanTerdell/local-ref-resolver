{
  "name": "@stefanterdell/local-ref-resolver",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "dev": "jest --watch",
    "build": "rimraf dist && tsc && node copyPackageFiles.js",
    "dryrun": "jest && npm run build && npm pub ./dist/ --dry-run"
  },
  "author": "Stefan Terdell",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^26.0.24",
    "@types/json-schema": "^7.0.9",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.2",
    "typescript": "^4.4.3"
  },
  "types": "index.d.ts",
  "repository": "git://github.com/stefanterdell/react-cron-editor.git",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}